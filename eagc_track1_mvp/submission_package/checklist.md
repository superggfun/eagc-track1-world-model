# Submission Readiness Checklist

This checklist maps the current local MVP to likely EAGC qualification submission needs.

| Item | Current status | Notes |
|---|---|---|
| Docker image | Prepared | `eagc-track1-agent:v0.11` built and tested with `docker-smoke`; image excludes Qwen model weights. |
| Dockerfile | Prepared | `Dockerfile` packages agent code only and expects an external vLLM endpoint. |
| Executable system | Partial local MVP available | LocalSim, mock, visual sequence, and visual-local hybrid paths run locally. |
| Source package | Prepared | `dist/eagc_track1_mvp_source.zip` passes clean reproducibility check. |
| `world_model.json` | Available | Generated by main demo runs and smoke tests. |
| `episode_log.jsonl` | Available | Generated by main demo runs and smoke tests. |
| `run_audit.json` | Available | Includes run metadata, validation status, and Qwen call counts. |
| Technical report | Draft available | Markdown draft prepared; PDF is prepared if local PDF backend is available, otherwise HTML/status output is generated. |
| Training disclosure | Prepared | See `training_resource_disclosure.md`. |
| Reproducibility statement | Prepared | See `reproducibility_statement.md`. |
| Open-source statement | Prepared draft | Source zip is prepared; final public release choice depends on competition requirements. |
| Official runtime | Not required yet / not provided | Current architecture keeps adapters replaceable. |
| Qualification submission portal | Pending organizer clarification | This package is pre-submission readiness material. |
| Model weights in Docker | Not included | Qwen model weights are served outside the agent image. |
| Model weight packaging | Pending organizer clarification | Need final rule for external endpoint, mounted weights, or bundled weights. |
| Official container/runtime schema | Pending organizer clarification | Needs organizer clarification when available. |
| Docker mock smoke | Available | `python tools/run_test_suite.py --tier docker-smoke`. |
| AI2-THOR / ProcTHOR | Not validated | Blocked by platform/runtime issue; not in stable path. |
| Training | None | No model training or fine-tuning has been performed. |
| Hardware disclosure | Available | RTX 5090 32GB local development/testing. |
| Test tiers | Prepared | `fast`, `targeted-text`, `targeted-vision`, `targeted-local-sim`, `targeted-track1`, `targeted`, `standard`, and `full` are available. |
| Fast tier | Stable | Deterministic; no real Qwen, real vision, local images, external simulators, or real ALFRED data. |
| ALFRED adapter | Fixture validated | Synthetic fixture conversion passes; real ALFRED dataset conversion is not yet validated. |
| VirtualHome manual-play smoke | Validated | Manual-play Windows connection, 8080 communication, scene graph extraction, 4/4 fixed household tasks, converted world model/log, frame export, single-frame Qwen vision, and multi-frame grounding have succeeded. |
| VirtualHome automated startup | Not validated | Manual Play is still required. |
| VirtualHome outputs in git | Excluded | Frames, raw Qwen responses, and `outputs/virtualhome_spike/` artifacts are runtime diagnostics and should remain ignored. |
| Pre-submission audit | Prepared | `python tools/pre_submission_audit.py`. |
